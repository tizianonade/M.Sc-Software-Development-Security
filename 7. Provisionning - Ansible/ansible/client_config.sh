#!/bin/bash

# #$1 = name of container

# echo "New user for client " $1
# echo "Name: "
# read name

# lxc exec $1 -- useradd -m $name
# lxc exec $1 -- passwd $name
# lxc exec $1 -- usermod $name --shell /bin/bash
# lxc exec $1 -- usermod -aG sudo $name

